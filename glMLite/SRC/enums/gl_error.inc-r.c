  {
    if (_gl_error == GL_NO_ERROR)	 gl_error =  0;
    if (_gl_error == GL_INVALID_ENUM)	 gl_error =  1;
    if (_gl_error == GL_INVALID_VALUE)	 gl_error =  2;
    if (_gl_error == GL_INVALID_OPERATION)	 gl_error =  3;
    if (_gl_error == GL_STACK_OVERFLOW)	 gl_error =  4;
    if (_gl_error == GL_STACK_UNDERFLOW)	 gl_error =  5;
    if (_gl_error == GL_OUT_OF_MEMORY)	 gl_error =  6;
    if (_gl_error == GL_TABLE_TOO_LARGE)	 gl_error =  7;
  }
